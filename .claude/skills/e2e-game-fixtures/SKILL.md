---
name: e2e-game-fixtures
description: Patterns for cy.loadGameFixture and assertGameState in Cuttle Cypress tests. Use whenever writing or modifying a test that calls either of those helpers — especially when the test specifies a short deck:, asserts on scrap, or draws the deck to empty. Covers the deck/scrap side-effect, the deck:[] hand-reveal rule, and the subset-check semantics of scrap.
---

Read `docs/patterns/e2e-game-fixtures.md` before proceeding.
