---
name: e2e-card-selectors
description: Data-attribute selector conventions for clicking cards, move choices, dialogs, and scrims in Cuttle Cypress tests. Use when a test needs to interact with the player-side UI via cy.get('[data-...]') — covers the rank-suit encoding (dash format), data-move-choice values, dialog IDs, and waiting scrim selectors.
---

Read `docs/patterns/e2e-card-selectors.md` before proceeding.
